# ChangeLog
- 统一视觉/图片生成与主对话模型的上游配置：`VisionService` 与图片生成不再读取独立的 `VISION_API_KEY/IMAGE_GEN_API_KEY`，而是强制复用 `AIAssistantConfig.api_key/base_url`（单一 ModelScope 上游与密钥）
- 移除视觉/图片生成的硬编码默认 token 回退，避免“看起来能跑但实际在用隐藏密钥”的不可审计行为

# System Map Delta
- Modules touched: src/plugins/nonebot_plugin_manager/config.py
- Entities touched: None
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: VisionService.* -> config.vision_api_key/base_url；图片生成 -> config.image_gen_api_key
- Docs updated: README/ARCH/DOMAIN (N/N/N)

# Files changed
- Updated `src/plugins/nonebot_plugin_manager/config.py`：将 `vision_api_key/vision_api_base_url/image_gen_api_key` 设为 `api_key/base_url` 的派生值，保证单一上游与单一密钥
- Added `docs/changesets/CHANGESET_20260206_05.md`：本次变更记录

# API Contract Diff
- None

# Invariants Report
- None

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `.venv\\Scripts\\python.exe -m py_compile src/plugins/nonebot_plugin_manager/config.py src/application/services/vision_service.py`
  - 结果：通过
- `.venv\\Scripts\\python.exe -c "import bot; from src.plugins.nonebot_plugin_manager.config import config; print(config.vision_api_key==config.api_key, config.vision_api_base_url==config.base_url, config.image_gen_api_key==config.api_key)"`
  - 结果：输出 `True True True`（证明视觉/图片生成与主模型完全复用同一 key/base_url）

# Risk Radar
- 串数据: No，仅调整配置来源，不涉及数据读写。
- 悬空引用: Yes，如果你之前只配置了 `VISION_API_KEY/IMAGE_GEN_API_KEY` 而没有配置主 `api_key`，现在会一起失效（需要把 token 放到 `MODELSCOPE_API_KEY/MODELSCOPE_TOKEN` 或 `MODEL_API_KEY`）。
- 幂等: No，不涉及副作用与幂等键。
- 性能: No，仅在启动时多一次字段赋值。
- 状态机: No，不涉及 session flow/step/TTL。
- 副作用: No，不改变副作用类型与执行点；仅改变鉴权配置来源。


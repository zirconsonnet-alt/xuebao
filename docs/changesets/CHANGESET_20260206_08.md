# ChangeLog
- 工具调用后不再让 LLM 二次“润色”输出，直接返回工具真实结果摘要，避免出现“工具失败但口头说已完成”的不一致行为
- 消息发送前统一剥离 `<think>/<analysis>` 思考段落（含只出现 `</think>` 的异常格式），避免机器人在群里泄露门禁/思考过程

# System Map Delta
- Modules touched: src/application/services/ai_assistant.py
- Entities touched: None
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: AIAssistant._handle_tool_calls；AIAssistant.send
- Docs updated: README/ARCH/DOMAIN (N/N/N)

# Files changed
- Updated `src/application/services/ai_assistant.py`：
  - 新增 `_strip_model_thought`，在 `send()` 前清理 `<think>/<analysis>` 段落
  - `_handle_tool_calls` 改为：写回 tool_calls/tool 真实消息到历史，但用户回复直接使用工具结果摘要，不再二次调用 LLM
- Added `docs/changesets/CHANGESET_20260206_08.md`：本次变更记录

# API Contract Diff
- None

# Invariants Report
- None

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `.venv\\Scripts\\python.exe -m py_compile src/application/services/ai_assistant.py`
  - 结果：通过
- `.venv\\Scripts\\python.exe -c "import bot; print('bot imported under venv')"`
  - 结果：启动与插件加载通过

# Risk Radar
- 串数据: No，仅修改对话输出与工具结果回传策略，不涉及数据写入。
- 悬空引用: No，函数为内部实现变更。
- 幂等: No，不涉及幂等键/写操作语义。
- 性能: No，减少一次工具后续 LLM 调用，整体更省。
- 状态机: No，不涉及 session flow/step/TTL。
- 副作用: Yes，工具成功时将更少出现“多余解释文本”；工具失败时会直接暴露失败原因（符合一致性目标）。


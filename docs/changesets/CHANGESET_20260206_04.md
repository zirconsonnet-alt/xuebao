# ChangeLog
- AI 调用上游返回 401 时不再让异常向上传播导致服务报错：在 `call_api` 捕获 `AuthenticationError`，返回可读的配置提示（ModelScope/OpenAI/自建网关）

# System Map Delta
- Modules touched: src/application/services/ai_assistant.py
- Entities touched: None
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: AIService.handle_ai_message -> AIAssistant.call_api
- Docs updated: README/ARCH/DOMAIN (N/N/N)

# Files changed
- Updated `src/application/services/ai_assistant.py`：新增 `AuthenticationError` 捕获与用户可读提示，避免 401 直接打断对话流程
- Added `docs/changesets/CHANGESET_20260206_04.md`：本次变更记录

# API Contract Diff
- None

# Invariants Report
- None

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `.venv\\Scripts\\python.exe -m py_compile src/application/services/ai_assistant.py`
  - 结果：通过

# Risk Radar
- 串数据: No，仅增加异常处理分支，不涉及数据读写。
- 悬空引用: No，仅新增 openai 异常类型导入与捕获。
- 幂等: No，不涉及副作用与幂等键。
- 性能: No，仅在异常路径增加字符串拼接与一次日志输出。
- 状态机: No，不涉及 session flow/step/TTL。
- 副作用: No，不改变任何副作用执行点；仅把 401 变为可读提示。


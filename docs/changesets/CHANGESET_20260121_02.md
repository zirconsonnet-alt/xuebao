# ChangeLog
- 修正 AIService 对狼人杀插件的导入路径，匹配 vendors 目录结构，避免加载时报错。

# System Map Delta
- Modules touched: application/services, vendors/nonebot_plugin_werewolf
- Entities touched: None
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: AIService (service_action handlers)
- Docs updated: README/ARCH/DOMAIN (N/N/N)

# Files changed
- src/application/services/ai_service.py: 修正 PlayerRegistry 导入路径。
- docs/changesets/CHANGESET_20260121_02.md: 记录本次变更。

# API Contract Diff
- None

# Invariants Report
- None（本次仅为导入路径修正，未涉及 INV 变更与约束）

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `python -c "import src.application.services.ai_service"`：失败，缺少依赖 `nonebot`。
- `python -c "import importlib; importlib.import_module('src.vendors.nonebot_plugin_werewolf.player_registry')"`：失败，缺少依赖 `nonebot`。
- 阻塞原因：本机环境未安装 `nonebot` 依赖。
- 最小替代验证步骤：确认目标文件存在 `src/vendors/nonebot_plugin_werewolf/player_registry.py`，并在依赖齐全环境运行上述导入命令。

# Risk Radar
- 串数据：No，导入路径修正不改变数据流。
- 悬空引用：No，改为现有 vendors 路径。
- 幂等：No，本次不涉及副作用。
- 性能：No，仅修改导入语句。
- 状态机：No，本次不涉及会话/流程。
- 副作用：No，不触及 DB/任务/外部调用。

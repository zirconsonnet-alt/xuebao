# ChangeLog
- 修复 AI 助手构建用户消息时的 `get_name_simple` 调用签名不兼容：支持历史调用 `get_name_simple(bot, event)`，避免运行期抛 `TypeError: ... takes 1 positional argument but 2 were given`

# System Map Delta
- Modules touched: src/interfaces/nonebot/utils.py
- Entities touched: None
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: AIService/AIAssistant（运行期获取群成员显示名）
- Docs updated: README/ARCH/DOMAIN (N/N/N)

# Files changed
- Updated `src/interfaces/nonebot/utils.py`：`get_name_simple(event)` → `get_name_simple(*args)`，兼容 `(event)` 与 `(bot, event)` 两种调用方式
- Added `docs/changesets/CHANGESET_20260206_03.md`：本次变更记录

# API Contract Diff
- None

# Invariants Report
- None

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `.venv\\Scripts\\python.exe -m py_compile src/interfaces/nonebot/utils.py`
  - 结果：通过
- `.venv\\Scripts\\python.exe -c "import nonebot; nonebot.init(); import inspect; from src.interfaces.nonebot.utils import get_name_simple; print('sig', inspect.signature(get_name_simple))"`
  - 结果：输出 `sig (*args: Any)`（证明已兼容多参数调用，避免 TypeError）

# Risk Radar
- 串数据: No，仅调整工具函数参数解析，不涉及任何数据写入。
- 悬空引用: No，新签名向后兼容 `(event)` 与 `(bot, event)`。
- 幂等: No，不涉及副作用与幂等键。
- 性能: No，仅增加一次轻量参数分支判断。
- 状态机: No，不涉及 session flow/step/TTL。
- 副作用: No，不改变任何外部调用或写入副作用。


# ChangeLog
- 修复图片生成端点与 base_url 拼接方式：图片生成 base_url 统一复用主 `base_url`（已含 `/v1`），并将请求路径从 `...base_url + v1/...` 调整为 `...base_url + /...`，避免出现 `.../v1v1/...` 与“文字能用、画图拿错上游”的配置漂移

# System Map Delta
- Modules touched: src/plugins/nonebot_plugin_manager/config.py; src/application/services/vision_service.py
- Entities touched: None
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: VisionService.generate_image_core -> _generate_image_api
- Docs updated: README/ARCH/DOMAIN (N/N/N)

# Files changed
- Updated `src/plugins/nonebot_plugin_manager/config.py`：`image_gen_api_base_url` 强制等于 `base_url`（单一上游）
- Updated `src/application/services/vision_service.py`：图片生成相关 URL 拼接改为 `/images/generations` 与 `/tasks/{id}`（不再手动拼 `v1/`）
- Added `docs/changesets/CHANGESET_20260206_06.md`：本次变更记录

# API Contract Diff
- None

# Invariants Report
- None

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `.venv\\Scripts\\python.exe -m py_compile src/plugins/nonebot_plugin_manager/config.py src/application/services/vision_service.py`
  - 结果：通过
- `.venv\\Scripts\\python.exe -c "import bot; from src.plugins.nonebot_plugin_manager.config import config; print(config.image_gen_api_base_url==config.base_url, config.image_gen_api_key==config.api_key)"`
  - 结果：输出 `True True`（证明图片生成与文字模型完全共用同一 key/base_url）

# Risk Radar
- 串数据: No，仅调整配置来源与 URL 拼接，不涉及数据写入。
- 悬空引用: Yes，如果上游网关不支持 `/images/generations`（OpenAI Images API），将返回 404/400；但这是“同一上游”的必然约束，需要上游能力匹配。
- 幂等: No，不涉及副作用与幂等键。
- 性能: No，不引入额外计算或 IO。
- 状态机: No，不涉及 session flow/step/TTL。
- 副作用: Yes，图片生成请求会切换到与文字相同的上游域名；需要确保上游授权包含图片生成能力。


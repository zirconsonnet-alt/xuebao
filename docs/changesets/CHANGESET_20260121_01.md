# ChangeLog
- 迁移 AI Assistant 核心到 application 层并新增 interfaces/nonebot 适配导出
- 移除 bytecode 过渡层与 root services 包，服务实现统一在 application/services
- 修正 application/services 绝对导入路径，补回 chat_service 真实实现

# System Map Delta
- Modules touched: application.services.ai_assistant; application.services.*; interfaces.nonebot.assistant; plugins.nonebot_plugin_manager.__init__
- Entities touched: None
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: nonebot plugin bootstrap -> application.services.service_manager
- Docs updated: README/ARCH/DOMAIN (N/Y/N)

# Files changed
- 新增：`src/application/services/ai_assistant.py`（AI Assistant 核心）
- 新增：`src/interfaces/nonebot/assistant.py`（Nonebot 适配导出）
- 修改：`src/application/services/*.py`（修正导入路径与恢复实现）
- 修改：`src/plugins/nonebot_plugin_manager/__init__.py`（改用 interfaces/nonebot 适配）
- 删除：`src/plugins/nonebot_plugin_manager/assistant.py`（移入 application 层）
- 删除：`src/services/**`（移除 bytecode 过渡层与 root services 包）
- 修改：`docs/ARCHITECTURE.md`（更新分层与助手位置）

# API Contract Diff
- None

# Invariants Report
- None（未新增/修改不变量）

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- 命令：`py -3.12 -m compileall src/application src/interfaces src/plugins src/core`
- 结果：通过

# Risk Radar
- 串数据：No（未改动数据读写逻辑）
- 悬空引用：Yes（移除 root services 后需持续核对导入路径）
- 幂等：No（幂等逻辑未改动）
- 性能：No（未新增关键路径）
- 状态机：No（流程未改动）
- 副作用：No（副作用逻辑未改动）

# ChangeLog
- 新增 DDD 目录骨架（domain/application/infrastructure/interfaces）并保留兼容层
- 迁移 metadata_commands 到 application.commands 并更新投票策略引用
- 同步架构与控制面文档中的路径说明

# System Map Delta
- Modules touched: application.commands.metadata_commands; services.metadata_commands; plugins.nonebot_plugin_manager.strategy.vote_strategy
- Entities touched: sessions, audit_events, audit_logs, idempotency_keys, vote_records
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: vote_strategy -> application.commands.metadata_commands
- Docs updated: README/ARCH/DOMAIN (N/Y/N)

# Files changed
- 新增：`src/domain/**`、`src/application/**`、`src/infrastructure/**`、`src/interfaces/**` 的包骨架与 `__init__.py`（DDD 分层落地起点）
- 新增：`src/application/commands/metadata_commands.py`（领域写入入口迁移）
- 修改：`src/services/metadata_commands.py`（兼容层转发至 application.commands）
- 修改：`src/plugins/nonebot_plugin_manager/strategy/vote_strategy.py`（改用 application.commands）
- 修改：`docs/ARCHITECTURE.md`、`docs/CONTROL_MAP.md`（同步分层与写入路径）

# API Contract Diff
- None

# Invariants Report
- None（未新增/修改不变量）

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- 命令：`python -m compileall src`
- 结果：失败（第三方 vendor 目录内已存在语法错误：`src/vendors/nonebot_plugin_werewolf/*.py`，与本次变更无关）
- 最小替代验证：确保本次新增/修改文件可被导入（`src/application/commands/metadata_commands.py`、`src/services/metadata_commands.py`）

# Risk Radar
- 串数据：No（仅目录与导入路径调整）
- 悬空引用：Yes（新增分层后若有未更新的旧引用可能遗漏）
- 幂等：No（幂等写入逻辑未变）
- 性能：No（未引入新计算路径）
- 状态机：No（flow/step 逻辑未改动）
- 副作用：No（写入行为未变，仅迁移路径）

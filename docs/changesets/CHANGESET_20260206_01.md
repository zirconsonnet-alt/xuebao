# ChangeLog
- 修复 `src/application/strategies/__init__.py` 误导出 `VoteController` 导致的 ImportError（`VoteController` 实际定义在 `src/interfaces/nonebot/vote_runtime.py`）
- 恢复 Nonebot 启动期对 `nonebot_plugin_internal_api` 的正常导入链路（不再被 strategies 包导入错误中断）

# System Map Delta
- Modules touched: src/application/strategies/__init__.py
- Entities touched: None
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: nonebot plugin import（启动期）
- Docs updated: README/ARCH/DOMAIN (N/N/N)

# Files changed
- Updated `src/application/strategies/__init__.py`：移除不存在的 `VoteController` re-export，避免包导入即异常
- Added `docs/changesets/CHANGESET_20260206_01.md`：本次变更记录

# API Contract Diff
- None

# Invariants Report
- None

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `python -m py_compile src/application/strategies/__init__.py`
  - 结果：通过
- `python -c "import bot; print('bot imported (global python)')"`
  - 结果：启动期不再出现 `VoteController` ImportError；日志显示 `nonebot_plugin_internal_api` 可被导入

# Risk Radar
- 串数据: No，仅修复导入导出列表，不涉及数据读写。
- 悬空引用: No，仓库内未发现从 `src.application.strategies` 导入 `VoteController` 的使用点。
- 幂等: No，不涉及副作用与幂等键。
- 性能: No，不引入额外计算，仅减少启动期异常。
- 状态机: No，不涉及 session flow/step 逻辑。
- 副作用: No，不改变任何执行副作用的路径，仅修复启动期导入。


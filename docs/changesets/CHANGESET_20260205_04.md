# ChangeLog
- 修复 ActivityService 的类型标注：将 `Activity`/`Application`/`ActivityService` 的 `group` 参数类型从 `Group` 调整为 `GroupContext`，避免静态检查告警（运行期实际注入为 GroupContext）。

# System Map Delta
- Modules touched: src/application, docs
- Entities touched: ActivityService, Activity, Application
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: None
- Docs updated: README/ARCH/DOMAIN (N/N/N)

# Files changed
- Updated `src/application/services/activity_service.py`：修正 group 类型注解与 import（`Group` → `GroupContext`）。
- Added `docs/changesets/CHANGESET_20260205_04.md`：本次变更记录。

# API Contract Diff
- None

# Invariants Report
- None

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `python -m py_compile src/application/services/activity_service.py`：通过

# Risk Radar
- 串数据: No，纯类型标注变更不涉及数据读写。
- 悬空引用: No，仅调整 import 路径与类型注解。
- 幂等: No，不影响幂等语义。
- 性能: No，不影响运行时路径。
- 状态机: No，不影响活动状态机逻辑。
- 副作用: No，不影响任何副作用执行点。


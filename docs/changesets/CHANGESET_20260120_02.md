# ChangeLog
- 修复迁移后的 service 源码不可编译问题，改为加载历史 bytecode 以确保启动
- application/services 全量改为兼容包装层，统一回落到 services 实现
- 更新 Nonebot 入口与 ai_tools 桥接的 import 路径，避免旧路径断裂

# System Map Delta
- Modules touched: services._pyc_loader; services.*; application.services.*; core.ai_tools.service_bridge; plugins.nonebot_plugin_manager.__init__; plugins.nonebot_plugin_manager.assistant
- Entities touched: None
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: nonebot plugin bootstrap -> application.services.service_manager
- Docs updated: README/ARCH/DOMAIN (N/Y/N)

# Files changed
- 新增：`src/services/_pyc_loader.py`（加载历史 bytecode 的兼容入口）
- 新增：`src/services/_pyc/*.pyc`（迁移前的服务字节码备份）
- 修改：`src/services/*.py`（改为 bytecode 加载适配）
- 修改：`src/application/services/*.py`（改为兼容包装层）
- 修改：`src/plugins/nonebot_plugin_manager/__init__.py`、`src/plugins/nonebot_plugin_manager/assistant.py`（更新 service_manager 引用路径）
- 修改：`src/core/ai_tools/service_bridge.py`（更新 BaseService 引用路径）
- 修改：`docs/ARCHITECTURE.md`（标注迁移期 bytecode 兼容）

# API Contract Diff
- None

# Invariants Report
- None（未新增/修改不变量）

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- 命令：`py -3.12 -m compileall src/application src/services src/plugins/nonebot_plugin_manager src/core`
- 结果：通过

# Risk Radar
- 串数据：No（未改动数据读写逻辑）
- 悬空引用：Yes（迁移期包装层路径依赖较多，需持续核查）
- 幂等：No（幂等逻辑未改动）
- 性能：Yes（加载 bytecode 多一层间接，需观察启动影响）
- 状态机：No（流程未改动）
- 副作用：No（写入行为未改动）

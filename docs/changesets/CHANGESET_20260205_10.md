# ChangeLog
- 将 `src/services/metadata_commands.py` 调整为稳定门面（re-export），实现下沉到 `src/application/commands/metadata_commands.py`，与分层目录保持一致
- `metadata_commands` 移除运行时对 `GroupDatabase/InternalDatabase` 的 import 依赖（仅 `TYPE_CHECKING`），避免在未初始化 Nonebot 时 import 触发插件加载/循环依赖

# System Map Delta
- Modules touched: src/services/metadata_commands.py; src/application/commands/metadata_commands.py; docs/ARCHITECTURE.md
- Entities touched: None
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: src.services.metadata_commands（门面）；src.application.commands.metadata_commands（实现）
- Docs updated: README/ARCH/DOMAIN (N/Y/N)

# Files changed
- Added `src/application/commands/metadata_commands.py`：写操作命令实现（sessions/audit/idempotency/vote/topics/activities/bot）
- Updated `src/services/metadata_commands.py`：作为门面 re-export，保持既有 `from src.services import metadata_commands` 调用不变
- Updated `docs/ARCHITECTURE.md`：补充“门面/实现”两层入口说明，避免路径歧义
- Added `docs/changesets/CHANGESET_20260205_10.md`：本次变更记录

# API Contract Diff
- None

# Invariants Report
- None

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `python -m py_compile src/application/commands/metadata_commands.py src/services/metadata_commands.py`
  - 结果：通过
- `python -c "from src.services import metadata_commands as m; print('imported', m.add_topic.__name__)"`
  - 结果：输出 `imported add_topic`
- `rg -n "src\\.application\\.commands\\.metadata_commands" -S src`
  - 结果：仅 `src/services/metadata_commands.py` 命中（证明实现仅由门面承接，未扩散到业务侧）

# Risk Radar
- 串数据: No，本次仅模块归位与 import 侧重构，不改变任何写入 SQL/参数。
- 悬空引用: Yes，若存在仓库外脚本直接引用旧实现位置（如历史路径）可能需要同步调整。
- 幂等: No，幂等键/写入逻辑未改动。
- 性能: No，门面仅一次 import 代理，不引入额外 IO/循环。
- 状态机: No，session flow/step/version/TTL 逻辑未改动。
- 副作用: No，副作用类型与执行点不变。


# ChangeLog
- 投票策略拆分为纯核心（application/strategies/vote_core.py）与 NoneBot 运行时适配（interfaces/nonebot/vote_runtime.py）
- application 层移除 NoneBot/Alconna 依赖，VoteService 改为调用运行时适配入口
- plugins 侧保留 vote_strategy 兼容 re-export

# System Map Delta
- Modules touched: src/application/strategies/vote_core.py; src/interfaces/nonebot/vote_runtime.py; src/application/services/vote_service.py; src/application/strategies/__init__.py; src/application/strategies/vote_strategy.py; src/plugins/nonebot_plugin_manager/strategy/vote_strategy.py
- Entities touched: sessions, idempotency_keys, vote_records, audit_events
- Invariants touched (INV-xxx): INV-001, INV-002, INV-003
- Command/Query entrypoints touched: VoteService.start_*; VoteController.vote
- Docs updated: README/ARCH/DOMAIN (N/Y/N)

# Files changed
- src/application/strategies/vote_core.py：新增投票纯核心（无框架依赖）
- src/interfaces/nonebot/vote_runtime.py：新增 NoneBot 运行时交互与副作用执行
- src/application/strategies/__init__.py：改为导出 core
- src/application/strategies/vote_strategy.py：改为 core re-export
- src/application/services/vote_service.py：改为调用 vote_runtime.VoteController
- src/plugins/nonebot_plugin_manager/strategy/vote_strategy.py：兼容 re-export + DeprecationWarning
- docs/ARCHITECTURE.md：记录策略核心与运行时适配位置
- docs/changesets/CHANGESET_20260123_14.md：变更记录

# API Contract Diff
- None

# Invariants Report
- INV-001：sessions.session_key 全局唯一；enforcement=DB PK(sessions.session_key)
- INV-002：vote_records 同一 session_key + user_id 只允许一条；enforcement=DB PK(vote_records.session_key,user_id)
- INV-003：idempotency_keys.idem_key 全局唯一；enforcement=DB PK(idempotency_keys.idem_key)

# DB Migration & Backfill Plan
- Migration: None
- Upgrade: None
- Backfill: None
- Rollback: 恢复 application/strategies/vote_strategy.py 原实现并回退 VoteService 导入

# Tests & Evidence
- Command: `rg -n "nonebot|onebot|alconna" src/application/strategies -n`
- Result: 无匹配结果
- Command: `rg -n "plugins\\.nonebot_plugin_manager\\.strategy" -S src/application src/core src/infrastructure`
- Result: 无匹配结果
- Command: `python -m compileall src`
- Result: 通过

# Risk Radar
- 串数据: No，未改动持久化路径
- 悬空引用: No，兼容 re-export 保留旧路径
- 幂等: No，幂等与会话逻辑保持不变
- 性能: No，运行时路径等价
- 状态机: No，会话流程未改变
- 副作用: Yes，运行时适配拆分可能影响日志/异常栈位置

# ChangeLog
- 将 `src/vendors/nonebot_plugin_wordcloud/__init__.py` 清空为“无副作用导入”：不再在 import 时注册 matcher/定时任务，避免 vendors 插件自行接管入口
- 新增 application `WordcloudService`：由 `nonebot_plugin_manager` 统一注册命令响应器，并通过群配置开关控制词云功能
- 词云生成逻辑复用 vendored `nonebot_plugin_wordcloud` 的 `get_wordcloud`，并将其对 `wordcloud.WordCloud` 的依赖改为使用仓库内 vendored `src.vendors.wordcloud`
- 补齐运行依赖：新增 `jieba`、`pillow`，并更新 `poetry.lock`
- `nonebot_plugin_manager`：支持 `service_action` 的 `aliases`，并在启动恢复 `wordcloud_daily_*` 定时回调
- CONTROL_MAP：补充词云入口与副作用（mask 文件/定时任务配置）

# System Map Delta
- Modules touched: src/vendors/nonebot_plugin_wordcloud / src/application/services / src/plugins/nonebot_plugin_manager / docs
- Entities touched: Services.Wordcloud, WordcloudService
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: on_command(service_action)；定时回调 init_scheduled_callbacks
- Docs updated: README/ARCH/DOMAIN (N/N/N)

# Files changed
- Updated `src/vendors/nonebot_plugin_wordcloud/__init__.py`：移除 import 时的 matcher/定时任务注册副作用（改为由 application service 驱动）
- Updated `src/vendors/nonebot_plugin_wordcloud/data_source.py`：`WordCloud` import 改为 `from src.vendors.wordcloud import WordCloud`，避免依赖 sys.path 注入
- Added `src/application/services/wordcloud_service.py`：新增词云服务入口（生成/历史/形状/每日定时）与开关
- Updated `src/application/enums.py`：新增 `Services.Wordcloud`
- Updated `src/application/services/service_manager.py`：注册 `WordcloudService`
- Updated `src/application/services/base_service.py`：`service_action` 支持 `aliases`；避免运行期直接 import `GroupContext` 以降低循环依赖风险
- Updated `src/plugins/nonebot_plugin_manager/__init__.py`：on_command 注册 aliases；启动恢复 `wordcloud_daily_*` 定时回调
- Updated `pyproject.toml`：新增 `jieba`、`pillow`
- Updated `poetry.lock`：依赖锁随 `pyproject.toml` 更新
- Updated `docs/CONTROL_MAP.md`：补充词云入口与副作用
- Added `docs/changesets/CHANGESET_20260205_06.md`：本次变更记录

# API Contract Diff
- None（外部 HTTP endpoint 未变更；内部命令入口新增：/今日词云 等由 service_action 接管）

# Invariants Report
- None

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `poetry lock`
  - 结果：成功写入 lock 文件
- `poetry install --sync`
  - 结果：安装 `jieba`；其余依赖按 lock 同步
- `poetry run python -m py_compile src/application/services/wordcloud_service.py src/application/services/base_service.py src/vendors/nonebot_plugin_wordcloud/__init__.py src/vendors/nonebot_plugin_wordcloud/data_source.py src/plugins/nonebot_plugin_manager/__init__.py`
  - 结果：通过
- `poetry run python -c "import src.vendors.nonebot_plugin_wordcloud as m; print('wordcloud_vendor_import_ok', m.__file__)"`
  - 结果：通过；证明 vendors 包 import 不再触发 NoneBot 侧副作用
- `poetry run python -c "import nonebot; nonebot.init(); import src.application.services.wordcloud_service as s; print('wordcloud_service_loaded')"`
  - 结果：通过；服务模块可在 NoneBot 初始化后加载

# Risk Radar
- 串数据: No，本次不涉及 DB 表结构与业务真相表写入。
- 悬空引用: Yes，若外部仍依赖 `nonebot_plugin_wordcloud` 原插件 matcher 行为（import 即可用），现在需改为使用 service 命令入口。
- 幂等: Yes，`data/scheduled_tasks.json` 以 task_id 覆盖写；重复“开启”会覆盖同一 task_id（期望行为需确认）。
- 性能: No，词云生成仍在原线程池执行路径（`get_wordcloud` run_in_executor）。
- 状态机: No，未引入新的 session flow。
- 副作用: Yes，新增 mask 文件写入与定时任务配置写入；失败语义已在服务层提示并退出。

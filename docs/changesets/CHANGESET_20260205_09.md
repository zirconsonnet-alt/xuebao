# ChangeLog
- 将 `metadata_commands` 收敛为仓库唯一实现：迁移到 `src/services/metadata_commands.py`，并删除 `src/application/services/metadata_commands.py` 与 `src/application/commands/metadata_commands.py`
- 全仓库更新引用到 `from src.services import metadata_commands`，确保“领域写操作集中入口”不在 `src/application/services` 下
- CONTROL_MAP：将副作用清单中对 `metadata_commands` 的真相源路径更新为 `src/services/metadata_commands.py`

# System Map Delta
- Modules touched: src/services / src/application / src/interfaces / src/infrastructure / src/plugins / docs
- Entities touched: None
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: None
- Docs updated: README/ARCH/DOMAIN (N/N/N)

# Files changed
- Added `src/services/metadata_commands.py`：唯一命令写入口实现（sessions/audit/idempotency/vote/topics/bot 内部鉴权等）
- Added `src/services/__init__.py`：服务包初始化
- Deleted `src/application/commands/metadata_commands.py`：旧实现文件（避免重复与路径歧义）
- Deleted `src/application/services/metadata_commands.py`：旧 re-export 门面（按要求不再位于 application/services）
- Updated `src/application/services/activity_service.py`：引用路径改为 `src.services.metadata_commands`
- Updated `src/application/strategies/vote_core.py`：同上
- Updated `src/interfaces/nonebot/vote_runtime.py`：同上
- Updated `src/infrastructure/persistence/topic_repository.py`：同上
- Updated `src/infrastructure/persistence/member_stats_repository.py`：同上
- Updated `src/plugins/nonebot_plugin_internal_api/__init__.py`：同上
- Updated `docs/CONTROL_MAP.md`：副作用清单真相源路径更新
- Added `docs/changesets/CHANGESET_20260205_09.md`：本次变更记录

# API Contract Diff
- None

# Invariants Report
- None

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `poetry run python -m py_compile src/services/metadata_commands.py`
  - 结果：通过
- `poetry run python -m py_compile src/application/services/activity_service.py src/interfaces/nonebot/vote_runtime.py src/plugins/nonebot_plugin_internal_api/__init__.py`
  - 结果：通过
- `rg -n "src\\.application\\.(services|commands)\\.metadata_commands" -S .`
  - 结果：无匹配（证明旧路径引用已清理）

# Risk Radar
- 串数据: No，本次仅模块迁移与 import 路径调整，不改变数据库写入逻辑。
- 悬空引用: Yes，若外部脚本/临时代码仍 import 旧路径会失败；仓库内引用已更新。
- 幂等: No，幂等行为实现未改动。
- 性能: No，不影响运行时性能。
- 状态机: No，不改变 session flow/step。
- 副作用: No，不改变副作用类型与执行点，仅改变调用入口路径。

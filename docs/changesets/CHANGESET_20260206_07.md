# ChangeLog
- 统一上游解析函数：文字生成（chat）、图片描述（vision chat）、图片生成（images/tasks）全部通过同一个 `get_upstream_context(model)` 获取 `base_url/api_key/headers/client`，实现“要么都能用，要么都不能用”的一致性入口
- 将 AI 相关配置从 `src/plugins/nonebot_plugin_manager/config.py` 迁移到 `src/settings/ai_assistant_config.py`，避免 import 时触发插件包初始化副作用造成的运行环境不一致；旧路径保留为 shim re-export

# System Map Delta
- Modules touched: src/core/upstream_factory.py; src/settings/ai_assistant_config.py; src/application/services/ai_assistant.py; src/application/services/vision_service.py; src/application/services/ai_service.py; src/plugins/nonebot_plugin_manager/config.py; docs/README.md
- Entities touched: None
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: AIAssistant.call_api；VisionService.describe_* / generate_image
- Docs updated: README/ARCH/DOMAIN (Y/N/N)

# Files changed
- Added `src/core/upstream_factory.py`：统一上游解析与 OpenAI client/header 构造（单一入口）
- Added `src/settings/ai_assistant_config.py`：AI 配置真相源（脱离插件包副作用）
- Added `src/settings/__init__.py`：settings 包初始化
- Updated `src/application/services/ai_assistant.py`：文字 LLM client 获取改走 `get_upstream_context`
- Updated `src/application/services/vision_service.py`：图片描述/图片生成的 base_url/api_key/header 获取改走 `get_upstream_context`
- Updated `src/application/services/ai_service.py`：TTS 配置引用迁移到 settings
- Added `src/plugins/nonebot_plugin_manager/config.py`：兼容 shim，re-export settings 配置
- Updated `docs/README.md`：配置入口路径更新
- Added `docs/changesets/CHANGESET_20260206_07.md`：本次变更记录

# API Contract Diff
- None

# Invariants Report
- None

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `.venv\\Scripts\\python.exe -m py_compile src/core/upstream_factory.py src/settings/ai_assistant_config.py src/application/services/ai_assistant.py src/application/services/vision_service.py src/application/services/ai_service.py src/plugins/nonebot_plugin_manager/config.py`
  - 结果：通过
- `.venv\\Scripts\\python.exe -c "import bot; print('bot imported under venv')"`
  - 结果：插件加载通过（用于验证迁移后仍可启动）

# Risk Radar
- 串数据: No，仅统一 client/header 构造与配置模块位置，不涉及数据读写。
- 悬空引用: Yes，若第三方代码强依赖旧配置模块内的具体字段（而非 `config`/导出函数），可能需要适配；仓库内已全量迁移。
- 幂等: No，不涉及幂等键/写操作语义。
- 性能: No，仅把重复的 key/base_url 解析集中，不增加额外 IO。
- 状态机: No，不涉及 session flow/step/TTL。
- 副作用: Yes，上游解析策略被统一后，图片/文字会严格共用同一套 env 规则；若之前靠“分开配置”绕过网关限制，现在会一起失败（符合“一致性”目标）。


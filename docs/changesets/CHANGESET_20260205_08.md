# ChangeLog
- 将 `src/vendors/nonebot_plugin_emojimix/__init__.py` 改为“无副作用导入”：不再在 import 时注册 on_message 响应器
- 新增 application `EmojimixService`：由 `nonebot_plugin_manager` 统一注册 on_message 响应器，并通过群配置开关控制 emoji 合成功能
- `service_message` 支持 `rule`：允许 message 级 handler 仅在匹配条件下触发，并按需 block（用于恢复 emojimix 的 “emoji+emoji” 精确匹配语义）
- CONTROL_MAP：补充 Emoji 合成入口与副作用（外部 HTTP 下载）

# System Map Delta
- Modules touched: src/vendors/nonebot_plugin_emojimix / src/application/services / src/plugins/nonebot_plugin_manager / docs
- Entities touched: Services.Emojimix, EmojimixService
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: on_message(service_message)
- Docs updated: README/ARCH/DOMAIN (N/N/N)

# Files changed
- Updated `src/vendors/nonebot_plugin_emojimix/__init__.py`：移除 import 时的响应器注册副作用（改为由 application service 驱动）
- Added `src/application/services/emojimix_service.py`：新增 Emoji 合成服务（匹配 `emoji+emoji`）
- Updated `src/application/enums.py`：新增 `Services.Emojimix`
- Updated `src/application/services/service_manager.py`：注册 `EmojimixService`
- Updated `src/application/services/base_service.py`：`service_message` 增加 `rule` 字段
- Updated `src/plugins/nonebot_plugin_manager/__init__.py`：on_message 注册 `rule`
- Updated `docs/CONTROL_MAP.md`：补充 Emoji 合成入口与副作用清单
- Added `docs/changesets/CHANGESET_20260205_08.md`：本次变更记录

# API Contract Diff
- None

# Invariants Report
- None

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `poetry run python -m py_compile src/application/services/emojimix_service.py src/application/services/base_service.py src/plugins/nonebot_plugin_manager/__init__.py src/vendors/nonebot_plugin_emojimix/__init__.py`
  - 结果：通过
- `poetry run python -c "import src.vendors.nonebot_plugin_emojimix as m; print('emojimix_vendor_import_ok', m.__file__)"`
  - 结果：通过；证明 vendors 包 import 不触发响应器副作用
- `poetry run python -c "import nonebot; nonebot.init(); import src.application.services.emojimix_service as s; print('emojimix_service_import_ok')"`
  - 结果：通过；服务模块可在 NoneBot 初始化后加载

# Risk Radar
- 串数据: No，本次不涉及 DB 表结构与数据写入。
- 悬空引用: Yes，若外部仍依赖 vendored emojimix 插件自动注册 matcher，现在需依赖 `EmojimixService` 的 message handler。
- 幂等: No，下载图片属于查询类行为，重复触发只会重复请求。
- 性能: Yes，匹配命中后会进行外部 HTTP 请求；网络慢时会变慢（已保持原插件 timeout）。
- 状态机: No，未引入新的 session flow。
- 副作用: Yes，外部 HTTP 下载 PNG；失败时返回错误提示。

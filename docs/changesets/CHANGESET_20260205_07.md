# ChangeLog
- 将 `src/vendors/nonebot_plugin_dialectlist/__init__.py` 改为“无副作用导入”：不再在 import 时注册 matcher/依赖 require
- 新增 application `DialectlistService`：由 `nonebot_plugin_manager` 统一注册命令响应器，并通过群配置开关控制 B话榜/看看B话 功能
- `pyproject.toml`：移除 pip 形态 `nonebot-plugin-dialectlist` 依赖与 `[tool.nonebot].plugins` 中的 `nonebot_plugin_dialectlist`，避免与 vendored 同名插件冲突
- 显式补齐可视化与用户信息依赖：新增 `nonebot-plugin-htmlrender`、`nonebot-plugin-userinfo`，并更新 `poetry.lock`
- CONTROL_MAP：补充 B话榜入口与副作用（头像下载/缓存/渲染）

# System Map Delta
- Modules touched: src/vendors/nonebot_plugin_dialectlist / src/application/services / src/application/enums.py / src/application/services/service_manager.py / pyproject.toml / docs
- Entities touched: Services.Dialectlist, DialectlistService
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: on_command(service_action)
- Docs updated: README/ARCH/DOMAIN (N/N/N)

# Files changed
- Updated `src/vendors/nonebot_plugin_dialectlist/__init__.py`：移除 import 时的 matcher/require 副作用（改为由 application service 驱动）
- Added `src/application/services/dialectlist_service.py`：新增 B话榜服务入口（B话榜/今日B话榜.../看看B话）与开关
- Updated `src/application/enums.py`：新增 `Services.Dialectlist`
- Updated `src/application/services/service_manager.py`：注册 `DialectlistService`
- Updated `pyproject.toml`：移除 `nonebot-plugin-dialectlist` 与 plugins 列表项；新增 `nonebot-plugin-htmlrender/nonebot-plugin-userinfo`
- Updated `poetry.lock`：依赖锁随 `pyproject.toml` 变更更新
- Updated `docs/CONTROL_MAP.md`：补充 B话榜入口与副作用清单
- Added `docs/changesets/CHANGESET_20260205_07.md`：本次变更记录

# API Contract Diff
- None（外部 HTTP endpoint 未变更；内部命令入口由 `DialectlistService` 接管：`/B话榜`、`/今日B话榜`、`/看看B话` 等）

# Invariants Report
- None

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `poetry lock`
  - 结果：成功写入 lock 文件
- `$env:POETRY_HTTP_TIMEOUT='300'; poetry sync`
  - 结果：安装/同步 `nonebot-plugin-htmlrender`、`nonebot-plugin-userinfo` 及其依赖；移除 `nonebot-plugin-dialectlist`
- `poetry run python -m py_compile src/application/services/dialectlist_service.py src/application/services/service_manager.py src/application/enums.py src/vendors/nonebot_plugin_dialectlist/__init__.py`
  - 结果：通过
- `poetry run python -c "import src.vendors.nonebot_plugin_dialectlist as m; print('dialectlist_vendor_import_ok', m.__file__)"`
  - 结果：通过；证明 vendors 包 import 不触发 NoneBot 侧副作用
- `poetry run python -c "import nonebot; nonebot.init(); import src.application.services.dialectlist_service as s; print('dialectlist_service_import_ok')"`
  - 结果：通过；服务模块可在 NoneBot 初始化后加载
- `poetry run python -c "import nonebot_plugin_dialectlist"`
  - 结果：`ModuleNotFoundError`（证明已不再依赖 pip 安装形态）

# Risk Radar
- 串数据: No，本次不涉及 DB 表结构与业务真相表写入。
- 悬空引用: Yes，若外部仍依赖 `nonebot_plugin_dialectlist` 原插件 matcher 行为（import 即可用），现在需改为使用 service 命令入口。
- 幂等: No，本次为查询/渲染类能力，不引入副作用幂等键。
- 性能: Yes，B话榜统计会做 DB 聚合查询 + 可选渲染；大群/长时间范围可能较慢。
- 状态机: No，未引入新的 session flow。
- 副作用: Yes，新增头像下载/缓存与可视化渲染（失败时降级文本输出）。

# ChangeLog
- 将 `nonebot_plugin_emojimix`（基于本地环境 `0.4.1`）从 pip 安装形态迁移为仓库内 vendored：`src/vendors/nonebot_plugin_emojimix/`
- `pyproject.toml`：移除 `nonebot-plugin-emojimix` 依赖，补齐其运行所需显式依赖 `emoji/httpx`
- `pyproject.toml`：从 `[tool.nonebot].plugins` 移除 `nonebot_plugin_emojimix`，改为由 `plugin_dirs = ["src/plugins", "src/vendors"]` 自动加载 vendored 插件，避免与 site-packages 同名插件冲突
- 更新 `poetry.lock` 以反映依赖变化

# System Map Delta
- Modules touched: 依赖与插件加载配置（pyproject.toml）/vendored 插件包（src/vendors/nonebot_plugin_emojimix）
- Entities touched: None
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: NoneBot `load_from_toml` 插件装载配置（`[tool.nonebot]`）
- Docs updated: README/ARCH/DOMAIN (N/N/N)

# Files changed
- 新增 `src/vendors/nonebot_plugin_emojimix/__init__.py`：vendored 插件代码（来源于本地 venv 安装包）
- 新增 `src/vendors/nonebot_plugin_emojimix/config.py`：同上
- 新增 `src/vendors/nonebot_plugin_emojimix/data_source.py`：同上
- 新增 `src/vendors/nonebot_plugin_emojimix/emoji_data.py`：同上
- 修改 `pyproject.toml`：移除 `nonebot-plugin-emojimix`；新增 `emoji/httpx`；移除 `[tool.nonebot].plugins` 中的 `nonebot_plugin_emojimix`
- 修改 `poetry.lock`：依赖锁文件随 `pyproject.toml` 变更更新
- 新增 `docs/changesets/CHANGESET_20260205_05.md`：本次变更记录

# API Contract Diff
- None

# Invariants Report
- None

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `poetry lock`
  - 结果：成功写入 lock 文件（无交互）
- `poetry install --sync`
  - 结果：移除环境中多余包 `nonebot-plugin-emojimix (0.4.1)`，确保不再依赖 pip 安装形态
- `poetry run python -c "import nonebot; nonebot.init(); from nonebot.plugin import load_plugin; load_plugin('src.vendors.nonebot_plugin_emojimix'); import src.vendors.nonebot_plugin_emojimix as m; print(m.__file__)"`
  - 关键证据：日志显示 `Succeeded to load plugin \"nonebot_plugin_emojimix\" from \"src.vendors.nonebot_plugin_emojimix\"`，并打印 `I:\\Projects\\xuebao\\src\\vendors\\nonebot_plugin_emojimix\\__init__.py`
  - 备注：该命令会出现 “No adapters found” 的 RuntimeWarning（此处未注册适配器，属于最小化导入验证场景）

# Risk Radar
- 串数据: No，本次不涉及 DB 读写与数据变更。
- 悬空引用: Yes，移除 pip 依赖后，若有代码/脚本直接 `import nonebot_plugin_emojimix` 可能受影响；当前 NoneBot 由 `src/vendors` 自动加载不受影响。
- 幂等: No，本次仅装载来源调整，不新增副作用动作。
- 性能: No，启动阶段多加载一个本地目录插件，量级不变。
- 状态机: No，未触及 flow/session 定义与推进语义。
- 副作用: Yes，插件装载来源切换为 vendored；若后续直接修改 vendored 代码可能带来行为差异。

# ChangeLog
- 修复 `EmojimixService` 的 `emojimix_rule` 规则函数参数注入：移除未使用的 `state: dict` 参数，避免 Nonebot 依赖注入解析失败导致 `nonebot_plugin_manager` 启动期导入报错

# System Map Delta
- Modules touched: src/application/services/emojimix_service.py
- Entities touched: None
- Invariants touched (INV-xxx): None
- Command/Query entrypoints touched: nonebot_plugin_manager 注册 on_message(rule=emojimix_rule)
- Docs updated: README/ARCH/DOMAIN (N/N/N)

# Files changed
- Updated `src/application/services/emojimix_service.py`：`emojimix_rule(event, state: dict)` → `emojimix_rule(event)`（state 未使用，去除以满足依赖注入约束）
- Added `docs/changesets/CHANGESET_20260206_02.md`：本次变更记录

# API Contract Diff
- None

# Invariants Report
- None

# DB Migration & Backfill Plan
- None

# Tests & Evidence
- `.venv\\Scripts\\python.exe -c "import bot; print('bot imported under venv')"`
  - 结果：`nonebot_plugin_manager` 成功加载，不再出现 `Unknown parameter state` 的 ValueError

# Risk Radar
- 串数据: No，仅调整规则函数签名，不涉及任何数据读写。
- 悬空引用: No，规则函数仅被装饰器元数据引用，签名变更不影响调用方。
- 幂等: No，不涉及副作用与幂等键。
- 性能: No，不引入额外计算。
- 状态机: No，不涉及 session flow/step/TTL。
- 副作用: No，不改变副作用类型与执行点。

